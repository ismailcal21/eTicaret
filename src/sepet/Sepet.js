import React from "react";
import { useSelector } from "react-redux";

const Sepet = () => {
  //   const produ = useSelector((state) => state.cart.items);
  //   console.log("cart", produ);

  return <div>sepet</div>;
};

export default Sepet;
